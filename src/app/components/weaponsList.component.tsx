import * as React from 'react';
import { listData } from '../interfaces/weaponsList.interface';
import { Link } from 'react-router-dom';

export class WeaponsList extends React.Component<listData> {
    render() {
        return (
            <div>AAAAAAAAAAAAAAAAAAAAA</div>
        );
    }
}